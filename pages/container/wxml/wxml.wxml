<view
        class="grid"

>
    <view class="item"
          wx:for="{{array}}"
    >
        {{index}}:{{item.message}}
    </view>
</view>

<view class="table">

    <block wx:for="{{array}}">
        <view class="table-th">{{item.message}}</view>
    </block>

</view>